# Core pipeline
docling>=2.99,<3            # PDF -> structured document + hierarchical chunking
sentence-transformers>=5.2  # local embeddings + cross-encoder reranker
weaviate-client>=4.20,<5    # vector database client (server runs via docker compose)
ollama>=0.6                 # local LLM for summarisation and section classification

# Web app
fastapi>=0.138
uvicorn>=0.49
python-multipart>=0.0.32    # multipart form parsing for PDF uploads
pydantic>=2.12
pymupdf>=1.27               # renders the PDF page behind a retrieved chunk
pillow>=10.0                # pdf_viewer builds the page image; pymupdf does
                            # not depend on it, so it must be declared here

# Config
python-dotenv>=1.2
